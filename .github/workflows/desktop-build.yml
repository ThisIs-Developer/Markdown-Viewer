name: Build Desktop App Binaries

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "lts/*"

      # PERF-022: Cache Neutralino binaries and downloaded CDN libraries
      - name: Cache Neutralino binaries
        uses: actions/cache@v4
        with:
          path: |
            desktop-app/bin
            desktop-app/resources/libs
          key: neutralino-${{ hashFiles('desktop-app/neutralino.config.json') }}

      - name: Setup Neutralinojs binaries
        working-directory: desktop-app
        run: npm run setup

      - name: Build desktop bundles
        working-directory: desktop-app
        run: npm run build

      - name: Stage release assets
        working-directory: desktop-app
        run: |
          VERSION="${GITHUB_REF_NAME}"
          STAGING="release-assets"
          mkdir -p "$STAGING"

          # Windows executable from the portable Neutralino bundle
          WIN_EXE="dist/markdown-viewer/markdown-viewer-win_x64.exe"
          if [ -f "$WIN_EXE" ]; then
            cp "$WIN_EXE" "$STAGING/markdown-viewer-win_x64.exe"
          fi

          # Portable bundle directory from neu build --release
          (cd dist && zip -r "../$STAGING/markdown-viewer-${VERSION}-portable.zip" markdown-viewer)

          # Source archive
          cd ..
          tar -czf "desktop-app/$STAGING/source.tar.gz" \
            --exclude='desktop-app/dist' \
            --exclude='desktop-app/bin' \
            --exclude='desktop-app/node_modules' \
            --exclude='desktop-app/output' \
            --exclude="desktop-app/$STAGING" \
            --exclude='.git' \
            desktop-app/
          cd desktop-app

          # SHA256 checksums
          cd "$STAGING"
          sha256sum * > SHA256SUMS.txt

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          name: "Markdown Viewer ${{ github.ref_name }}"
          generate_release_notes: true
          files: desktop-app/release-assets/*
